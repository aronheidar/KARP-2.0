# Cloudflare Workers (static assets) fyrir Astro-appið í web/ (karp21, root = web).
# ATH: main-worker (worker.js, /api/flug proxy) var BAKKAÐUR ÚT 2026-07-02 —
# CF-buildið féll með honum (líklega árekstur við deploy-skipun verkefnisins).
# worker.js liggur dormant í repo; virkja síðar gegnum CF-dashboard stillingar.
name = "karp21"
compatibility_date = "2026-06-29"

[assets]
directory = "./dist"
